# Backend URL for REST + Socket.IO
VITE_BACKEND_URL=http://localhost:5001

# Admin UI (development)
VITE_ADMIN_LOGIN=admin
VITE_ADMIN_PASSWORD=lumen2026

# Optional — use Ably instead of Socket.IO (set to "true" when configured)
VITE_USE_ABLY=false
VITE_ABLY_API_KEY=

# Optional — Supabase client (backend v2)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Netlify function — Apple/Google Wallet pass
WALLETWALLET_API_KEY=
